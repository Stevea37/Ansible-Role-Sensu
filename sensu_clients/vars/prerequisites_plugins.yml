---

base:
    - 'sensu-plugins-disk-checks'
    - 'sensu-plugins-load-checks'
    - 'sensu-plugins-memory-checks'

cdsrisk:
    - 'sensu-plugins-cdsrisk'

web:
    - 'sensu-plugins-web-response'

atlassian:
    - 'sensu-plugins-atlassian'

 
### DICT
plugins_ref: {} # EXAMPLE
                #MACHINE2: "{{ base + atlassian}}"
                #MACHINE3: "{{ base }}"
                #MACHINE4: "{{ base }}"
                #MACHINE5: "{{ base + web}}"
                #MACHINE6: "{{ base  + cdsrisk}}"
                #MACHINE7: "{{ base + atlassian }}"