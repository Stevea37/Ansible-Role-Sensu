{
  "client": {
    "name": "{{ ansible_hostname }}",
    "address": "{{ ansible_default_ipv4.address }}",
    "environment": "cds-data",
    "subscriptions": [{% for sub in subs_dict[ansible_hostname] -%}{% if loop.last %}
      "{{ sub }}"{% else %}
      "{{ sub }}",{% endif %}
{%- endfor %}
    ],
    "socket": {
      "bind": "127.0.0.1",
      "port": 3030
    }
  }
}
