---

- name: Installing Plugins
  gem:
    name: "{{ item }}"
    executable: "{{ gem_executable }}"
    repository: "{{ gem_repo }}"
  with_items: "{{ plugins }}"
  when: specify_gem_repo|bool == true


- name: Installing Plugins
  gem:
    name: "{{ item }}"
    executable: "{{ gem_executable }}"
  with_items: "{{ plugins }}"
  when: specify_gem_repo|bool == false